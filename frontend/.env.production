# Production environment variables
# These are baked into the frontend build at Docker build time

# API URL - empty means same origin (the frontend and API are served from same domain)
VITE_API_URL=''

# API Key - this is set at build time from environment via build arg
VITE_API_KEY=dashi-secret-key-2026
